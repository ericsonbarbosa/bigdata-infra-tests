---
mongo_service: mongod
mongo_port: 27017
mongo_conf: /etc/mongod.conf
mongo_shell_bin: mongosh          # use "mongo" se shell legado
mongo_expected_bind_ip: "127.0.0.1"   # vazio ("") pula o assert de bind
mongo_test_auth: false            # ligue quando houver auth habilitado
mongo_admin_user: admin
mongo_bind_external_check: true